GIT & GITHUB
GIT: GIT is a distributed version control system used to manage versions. It is developed and maintained by oracle
   steps to install
     1. goto google and type "git for windows"
     2. goto downlaods-git --> 64 bit windows setup
     3. double click--->yes--->next--->next
     4. check whether "vscode as default editor" or not?
      5. next--->next---> next until final install
     6. To verify, opene command prompt , write "git -v"

     open vs code--->terminal--->new terminal